// Clean Rollerblade Wheels is handled via right-click context menu only.
// B42.13 craftRecipe OnCreate only supports Java-side callbacks (RecipeCodeOnCreate),
// so Lua-based repair logic cannot be triggered from the crafting menu.
// Use: Right-click Rollerblades → Clean Rollerblade Wheels
//   Requires: Screwdriver (kept) + Toothbrush (kept) + AlcoholWipes (consumed)

module Rollerblades42
{
}